# Environment Variables for Redzone Fantasy
# Copy this file to .env.local and fill in your actual values

# Database Configuration (Required)
MONGODB_URI=mongodb://localhost:27017
MONGODB_DB_NAME=redzone_fantasy

# API Keys (Optional - only needed if you enable YouTube sources)
YOUTUBE_API_KEY=your_youtube_api_key_here

# Logging Configuration (Optional)
LOG_LEVEL=info

# Next.js Configuration (Required for production)
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your_nextauth_secret_here

# Cron Job Security (Required for scheduled ingestion)
CRON_SECRET=your_secure_random_string_here

# Future: Clerk Authentication (Optional - for when you add user auth)
# NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
# CLERK_SECRET_KEY=

# Instructions:
# 1. Copy this file to .env.local
# 2. Replace the placeholder values with your actual credentials
# 3. Make sure .env.local is in your .gitignore (it should be by default)
# 4. For production deployment on Vercel, add these as environment variables in your Vercel dashboard
